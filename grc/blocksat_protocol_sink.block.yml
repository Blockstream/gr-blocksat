# auto-generated by grc.converter

id: blocksat_protocol_sink
label: Blocksat Protocol Sink
category: '[Blockstream Satellite]/FIFO'

parameters:
-   id: blocks_pipe
    label: Blocks Pipe
    dtype: string
-   id: user_pipe
    label: User Data Pipe
    dtype: string
-   id: protocol_version
    label: Protocol Version
    dtype: int
    default: '1'
-   id: disable_api
    label: Disable API Data
    dtype: bool
    default: 'False'
-   id: disable_blocks
    label: Disable Blocks Data
    dtype: bool
    default: 'False'

inputs:
-   domain: message
    id: async_pdu

templates:
    imports: import blocksat
    make: blocksat.protocol_sink(${blocks_pipe}, ${user_pipe}, ${protocol_version},
        ${disable_api}, ${disable_blocks})

file_format: 1
