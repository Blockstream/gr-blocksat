# auto-generated by grc.converter

id: blocksat_ffw_coarse_freq_req_cc
label: Feed-forward Coarse Freq Recovery
category: '[Blockstream Satellite]/Synchronizers'

parameters:
-   id: fft_len
    label: FFT Length
    dtype: int
-   id: alpha
    label: Averaging Alpha
    dtype: float
-   id: M
    label: Mode
    dtype: int
    default: '2'
    options: ['2', '4']
    option_labels: [BPSK, QPSK]
    hide: none
-   id: sleep_per
    label: Sleep Period
    dtype: int
-   id: debug
    label: Debug
    dtype: bool
-   id: frame_len
    label: Frame Length
    dtype: int
-   id: sps
    label: Samples per Symbol
    dtype: int

inputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: start_index
    optional: true

outputs:
-   domain: stream
    dtype: complex
-   label: avg_mag_fft
    domain: stream
    dtype: float
    optional: true

templates:
    imports: import blocksat
    make: blocksat.ffw_coarse_freq_req_cc(${fft_len}, ${alpha}, ${M}, ${sleep_per},
        ${debug}, ${frame_len}, ${sps})
    callbacks:
    - get_frequency
    - reset

file_format: 1
