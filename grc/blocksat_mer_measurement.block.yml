# auto-generated by grc.converter

id: blocksat_mer_measurement
label: MER Estimator
category: '[Blockstream Satellite]/Measurement Tools'

parameters:
-   id: alpha
    label: Averaging Alpha
    dtype: float
    default: '0.0001'
-   id: M
    label: Constellation Order
    dtype: int
-   id: frame_len
    label: Frame Length
    dtype: int

inputs:
-   label: sym_in
    domain: stream
    dtype: complex

outputs:
-   label: mer
    domain: stream
    dtype: float
    optional: true

templates:
    imports: import blocksat
    make: blocksat.mer_measurement(${alpha}, ${M}, ${frame_len})
    callbacks:
    - get_snr()
    - set_alpha(${alpha})
    - enable()
    - disable()

file_format: 1
