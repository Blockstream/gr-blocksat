# auto-generated by grc.converter

id: blocksat_agc_cc
label: AGC
category: '[Blockstream Satellite]/Level Controllers'

parameters:
-   id: rate
    label: Rate
    dtype: real
    default: 1e-4
-   id: reference
    label: Reference
    dtype: real
    default: '1.0'
-   id: gain
    label: Gain
    dtype: real
    default: '1.0'
-   id: max_gain
    label: Max Gain
    dtype: real
    default: '65536'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex
-   label: level
    domain: stream
    dtype: float
    optional: true

templates:
    imports: import blocksat
    make: blocksat.agc_cc(${rate}, ${reference}, ${gain}, ${max_gain})
    callbacks:
    - set_rate(${rate})
    - set_reference(${reference})
    - set_gain(${gain})
    - set_max_gain(${max_gain})

file_format: 1
