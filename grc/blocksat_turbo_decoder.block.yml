# auto-generated by grc.converter

id: blocksat_turbo_decoder
label: Turbo Decoder
category: '[Blockstream Satellite]/Error Coding'

parameters:
-   id: K
    label: dataword_len
    dtype: int
-   id: pct_en
    label: Rate
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [1/2, 1/3]
-   id: n_ite
    label: Max Iterations
    dtype: int
-   id: flip_llrs
    label: Flip LLRs
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import blocksat
    make: blocksat.turbo_decoder(${K}, ${pct_en}, ${n_ite}, ${flip_llrs})

file_format: 1
